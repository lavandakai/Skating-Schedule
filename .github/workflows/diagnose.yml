name: Diagnose Sandy Hill Aug 3 cancellation

on:
  workflow_dispatch: {}

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect exceptionsHtmlId=0 (figure skate group)
        run: |
          python3 - <<'EOF'
          import json
          import urllib.request

          req = urllib.request.Request(
              "https://data.ottrec.ca/export/latest.json",
              headers={"User-Agent": "Mozilla/5.0"},
          )
          with urllib.request.urlopen(req, timeout=60) as resp:
              data = json.load(resp)

          html_by_id = {h["id"]: h["html"] for h in data["html"]}
          print("html for id=0:")
          print(html_by_id.get(0, "(missing)"))
          EOF
